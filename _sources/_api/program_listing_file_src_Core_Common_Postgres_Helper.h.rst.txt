
.. _program_listing_file_src_Core_Common_Postgres_Helper.h:

Program Listing for File Helper.h
=================================

|exhale_lsh| :ref:`Return to documentation for file <file_src_Core_Common_Postgres_Helper.h>` (``src/Core/Common/Postgres/Helper.h``)

.. |exhale_lsh| unicode:: U+021B0 .. UPWARDS ARROW WITH TIP LEFTWARDS

.. code-block:: cpp

   #pragma once
   
   #include <pqxx/pqxx>
   #include <utility>
   
   namespace Core::Common::Postgres {
   
   template <typename T, typename C>
   std::optional<T> getOptional(C const & cell)
   {
       if (!cell.is_null())
           return cell.template as<T>();
       else
           return std::nullopt;
   }
   
   template <typename T, typename C>
   T getOptional(C const & cell, T defaultValue)
   {
       if (!cell.is_null())
           return cell.template as<T>();
       else
           return defaultValue;
   }
   
   }  // namespace Core::Common::Postgres
