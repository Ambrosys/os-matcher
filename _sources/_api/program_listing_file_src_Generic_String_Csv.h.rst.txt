
.. _program_listing_file_src_Generic_String_Csv.h:

Program Listing for File Csv.h
==============================

|exhale_lsh| :ref:`Return to documentation for file <file_src_Generic_String_Csv.h>` (``src/Generic/String/Csv.h``)

.. |exhale_lsh| unicode:: U+021B0 .. UPWARDS ARROW WITH TIP LEFTWARDS

.. code-block:: cpp

   
   /*
    * Author: Fabian Sandoval Saldias (fabianvss@gmail.com)
    * Date: 2017-04-03
    */
   
   #pragma once
   
   #include <Generic/String/Trim.h>
   
   #include <sstream>
   #include <string>
   #include <vector>
   
   namespace Generic::String {
   
   std::vector<std::string> readNextRow(std::istream & str, char const separator)
   {
       std::vector<std::string> data;
       std::string line;
       std::getline(str, line);
   
       std::stringstream lineStream(line);
       std::string cell;
   
       while (std::getline(lineStream, cell, separator))
       {
           Generic::String::trim(cell);
           data.push_back(cell);
       }
       if (!lineStream && cell.empty())  // trailing comma
           data.emplace_back();
       return data;
   }
   
   }  // namespace Generic::String
